import { Auth } from "components/auth";
import "./style.css";

const HomeScreen = () => {
  return (
    <Auth>
      <div className="signup-container">HomeScreen</div>
    </Auth>
  );
};cccc

export { HomeScreen };

// HOC
