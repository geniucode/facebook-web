import { Auth } from "components/auth";
import "./style.css";

const ProfileScreen = () => {
  return (
    <Auth>
      <div className="signup-container">ProfileScreen</div>
    </Auth>
  );
};

export { ProfileScreen };

// HOC
